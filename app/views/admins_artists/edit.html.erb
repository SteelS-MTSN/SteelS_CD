<div class="container">
	<div class="col-md-2">
		<%= render 'admins/side' %>
	</div>
	<div class="col-md-8">
		<h1>アーティスト編集</h1>
		<br>
		<%= form_for(@artist, url: admins_artist_path) do |f| %>
			<%= f.text_field :name %>
		    <%= f.submit '決定' %>
		<% end %>
		<br>
		<%= link_to "戻る", admins_artists_path %>
	</div>
</div>