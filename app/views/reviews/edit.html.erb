<%= render 'admins/header' %>
	<div class="container-fluid bg-color-aqua">
	<div class= ' contaniner'>
		<div class= 'row'>
		<div class = 'col-smー8'>
			
			<div class = 'rex'>
			
			<h1>コメント編集</h1>
				<div class="center-block">
				<%= form_for(@review, url: {controller: "reviews", action: "update"}) do |f| %>
					<div class="form-group">
						<%= f.label "コメント" %>
						<br>
						<%= f.text_area :review_text, size: '50x10' %>
					</div> 
					<div class="form-group">
						<%= f.submit "完了" %>
					</div>
				</div>
			</div>
		</div>
		</div>
	</div>
	</div>
		<% end %>
		<%= render 'admins/fotter' %>

