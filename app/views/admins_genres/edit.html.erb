<div class="container">
	<div class="col-md-2">
		<%= render 'admins/side' %>
	</div>
	<div class="col-md-8">
		<h1>ジャンル編集</h1>
		<br>


		<%= form_for(@genre, url: admins_genre_path) do |f|  %>

		<%= f.text_field :genre_name %>

		<%= f.submit '決定'%>

		<% end %>
		<br>
		<%= link_to "戻る", admins_genres_path %>
	</div>
</div>