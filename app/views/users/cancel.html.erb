
<body class="is_quit_body">

	<div class="user_is_quit">
		<h1>退会確認</h1>




		<%= form_for(@user) do |f| %>

		<label><%= f.radio_button :is_quit, "false" %>退会しない</label>

		<label><%= f.radio_button :is_quit, "true"  %>退会する</label>

		<%= f.submit '決定' %>

		<% end %>
	</div>
</body>
